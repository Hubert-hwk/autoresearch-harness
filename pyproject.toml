[project]
name = "autoresearch-harness"
version = "0.1.0"
description = "A minimal autoresearch harness for business optimization trials."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
autoresearch = "autoresearch_harness.cli:main"

