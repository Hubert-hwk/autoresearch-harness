[project]
name = "autoresearch-harness"
version = "0.3.0.dev0"
description = "An auditable harness for agent-driven business and engineering optimization."
requires-python = ">=3.10"
dependencies = [
    "numpy==2.2.6",
]

[project.scripts]
autoresearch = "autoresearch_harness.cli:main"
